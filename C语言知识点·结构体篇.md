# C语言知识点 · 结构体篇

## 补充知识：

### 1、联合体（Union）

#### 📚 核心概念：共享内存 ####

​	联合体（Union）是一种特殊的数据类型，它允许不同的成员**共享同一块内存空间**。这意味着，**所有成员都从同一内存地址开始存储**，整个联合体的大小由其中最大的成员决定。

| 特性         | 联合体 (union)             | 结构体 (struct)              |
| :----------- | :------------------------- | :--------------------------- |
| **内存分配** | 所有成员共享内存           | 每个成员拥有独立内存         |
| **总大小**   | 最大成员大小（考虑对齐）   | 所有成员大小之和（考虑对齐） |
| **数据存储** | 同一时间只能存一个成员的值 | 可同时存储所有成员的值       |

#### ⚙️ 语法与操作 ####

- **声明与定义**：使用 `union`关键字，语法与结构体类似。
```c
union Data {    int i;    float f;    char str[20]; }; 
union Data data1; // 声明变量
```
- **初始化**：在C99及以上标准，可使用指定初始化器初始化**第一个**成员。
```c
union Data data2 = {.i = 10}; // 初始化
```
- **成员访问**：使用点运算符（`.`）访问成员，通过指针访问则使用箭头运算符（`->`）。

#### 💾 内存与大小计算 ####

​	联合体的大小并非简单地等于最大成员的大小，还需要遵循**内存对齐规则**：

1. 联合体的大小**至少**是最大成员的大小。
2. 当最大成员的大小不是所有成员中的**最大对齐数**的整数倍时，需要填充内存，使其对齐到该对齐数的整数倍。

例如，对于包含一个5字节char数组和一个int的联合体，其大小并非5字节。因为int的对齐数（例如4）是最大对齐数，5不是4的整数倍，所以需要对齐到8字节。

#### 🎯 典型应用场景 ####

1. **节省内存空间**：当一组数据项（如一个配置项的不同类型值）不会同时使用时，用联合体将它们组织在一起可有效节省内存，尤其在嵌入式系统中很有用。
2. **实现变体记录**：联合体常与结构体嵌套，创建一个能存储多种类型数据的结构。通常会有一个额外的标签字段（如枚举）来指示当前联合体中有效的数据类型。
3. **判断机器字节序**：利用联合体成员共享内存的特性，可以巧妙地判断当前系统是大端模式还是小端模式。
```c
int check_endian() 
{    
    union { int i; char c; } u;
	u.i = 1;    
    return u.c; // 返回1为小端，0为大端
}
```

#### ⚠️ 重要注意事项 ####

- **数据覆盖**：由于成员共享内存，**对一个成员赋值会覆盖其他成员的值**。因此，访问非当前写入的成员会导致未定义行为（得到的是对同一内存字节的不同解释）。
- **类型跟踪**：C语言本身不记录联合体当前存储的是哪个成员。程序员需要**自行跟踪当前哪个成员是有效的**，通常借助一个额外的类型标签变量。
- **可移植性**：联合体的内存对齐方式可能因编译器和平台而异，在涉及跨平台时需特别注意。



### 2、对齐

​	简单来说，内存对齐就像是规定书籍在书架上必须从特定格子的开头放置，这样拿取效率最高。在计算机里，它要求数据在内存中的起始地址必须是其自身大小的整数倍。

#### 🔍 对齐的核心原因 ####

​	内存对齐主要是为了**提升CPU访问内存的效率**。现代CPU通常按块（例如4字节或8字节）读取内存。如果数据“自然对齐”（例如一个4字节的`int`变量地址是4的倍数），CPU通常一次操作就能读完。如果数据未对齐，CPU可能需要进行两次访问和额外的拼接操作，这会降低效率，在某些硬件上甚至会导致程序错误。

#### ⚙️ 基本对齐规则 ####

​	对齐的具体规则由编译器自动实施，主要遵循以下原则：

1. **数据成员对齐**：结构体（`struct`）或联合体（`union`）中的每个成员，其起始地址必须是“自身大小”与“编译器当前对齐模数”中较小值的整数倍。
2. **结构体整体对齐**：在整个结构体的所有成员对齐后，结构体本身的总大小还需要是其“最宽基本类型成员大小”与“编译器当前对齐模数”中较小值的整数倍，不足部分会在末尾填充字节。



### 3、枚举 （enum）

​	枚举是C语言中一种非常有用的工具，它能将一组相关的整数常量组织在一起，让代码更清晰、更安全。

#### 📌 枚举的基本概念与语法 ####

​	枚举（Enumerated Type）是一种用户自定义的数据类型，用于定义一组命名的整型常量。它的核心目的是用有意义的符号名称来代替“魔法数字”，从而**提高代码的可读性和可维护性**。

​	定义一个枚举类型的基本语法如下：

```c
enum 枚举名 {
    枚举常量1,
    枚举常量2,
    // ...
    枚举常量N
};
```

​	例如，定义一个表示星期的枚举：

```c
enum Weekday {
    SUNDAY,    // 默认值为 0
    MONDAY,    // 默认值为 1
    TUESDAY,   // 默认值为 2
    WEDNESDAY, // ... 以此类推
    THURSDAY,
    FRIDAY,
    SATURDAY
};
```

- **默认赋值**：如果不显式指定值，枚举常量会从 **0** 开始自动递增。
- **显式赋值**：你也可以为常量指定特定的整数值，后续未指定的常量会自动按顺序递增。

```c
enum HttpStatusCode {
    HTTP_OK = 200,
    HTTP_NOT_FOUND = 404,
    HTTP_INTERNAL_ERROR = 500
};
```

- **混合赋值**：可以只对部分常量显式赋值。

```c
enum Example {
    A = 10,
    B,    // B 的值为 11
    C = 5,
    D     // D 的值为 6
};
```

#### ⚙️ 枚举变量的使用 ####

​	定义枚举类型后，就可以用它来声明变量了：

```c
enum Weekday today; // 先定义类型，后声明变量
enum Weekday { SUN, MON, TUE } day; // 定义类型的同时声明变量
enum { SUN, MON, TUE } day; // 匿名枚举（直接声明变量，省略枚举名）
```

**赋值与操作**：

- 只能将**枚举常量**赋给枚举变量，不能直接赋整数值（除非使用强制类型转换）。

```c
today = MONDAY; // 正确
// today = 1;   // 错误（在C语言中，虽然一些编译器可能只给出警告，但这不是好习惯）
today = (enum Weekday)1; // 通过强制类型转换赋值
```

- 枚举值可以用于**比较操作**（如 `==`, `>`, `<`）和 **`switch`语句**，这使得逻辑判断非常清晰。

#### 💡 枚举的常见应用场景 ####

​	枚举在以下场景中尤其有用：

1. **状态机（State Machine）**：清晰地表征对象的不同状态，如订单状态、交通灯状态。 `enum OrderState { PENDING, PAID, SHIPPED, COMPLETED };`
2. **错误码（Error Code）**：用有意义的名称代替数字错误码，便于理解和调试。 `enum ErrorCode { SUCCESS, NULL_POINTER, OUT_OF_MEMORY, FILE_NOT_FOUND };`
3. **配置选项（Configuration Options）**：如程序设置、模式选择等。 `enum LogLevel { DEBUG, INFO, WARNING, ERROR };`
4. **标志位（Flags）**：通过为常量赋值为2的幂（`1 << 0`, `1 << 1`等），可以用按位或(`|`)组合多个选项，按位与(`&`)检查选项。 `typedef enum {    PERM_READ = 1 << 0,    PERM_WRITE = 1 << 1,    PERM_EXECUTE = 1 << 2 } Permission; Permission myPerm = PERM_READ | PERM_WRITE; // 组合权限 if (myPerm & PERM_READ) { ... } // 检查读权限`

#### 🆚 枚举与宏定义的区别 ####

​	你可能会想，用 `#define`也能定义常量，为什么还要用枚举呢？它们的主要区别如下：

| 特性           | 枚举 (enum)                                            | 宏定义 (#define)             |
| -------------- | ------------------------------------------------------ | ---------------------------- |
| **关联性**     | 常量自动关联在同一类型下，是一个整体。                 | 常量彼此独立，缺乏内在联系。 |
| **类型检查**   | 提供一定的类型安全性，编译器会检查赋值是否為枚举類型。 | 简单的文本替换，无类型检查。 |
| **调试便利性** | 调试器中通常显示符号名称，更易读。                     | 调试器只显示数字，可读性差。 |
| **自动赋值**   | 支持自动递增，管理一组连续值更方便。                   | 每个值必须手动定义。         |

#### ⚠️ 重要注意事项与技巧 ####

1. **类型与范围**：在C语言中，枚举类型的变量通常被实现为 `int`类型。枚举变量的取值不严格限定在枚举常量的范围内，例如 `today = 100;`在语法上可能只导致警告而非错误，但这属于不良编程习惯，可能导致逻辑错误。
2. **作用域与命名**：枚举常量是全局可见的，因此不同枚举类型的常量名不能重复。通常使用**前缀**（如 `COLOR_RED`, `STATE_IDLE`）来避免命名冲突。
3. **使用typedef简化**：使用 `typedef`关键字可以为枚举类型创建一个别名，这样在声明变量时就不需要每次都写 `enum`关键字了，代码更简洁。
```c
typedef enum { RED, GREEN, BLUE } Color;
Color favoriteColor = BLUE; // 直接使用 Color，无需写 enum Color
```
4. **输出枚举名**：直接打印枚举变量得到的是其整数值。若想输出其符号名称，需要自己编写转换函数。
```c
const char* getColorName(Color c) {    
switch(c) {        
    case RED: return "RED";        
    case GREEN: return "GREEN";        
    case BLUE: return "BLUE";        
    default: return "UNKNOWN";    
	} 
}
printf("Color: %s\n", getColorName(favoriteColor));
```

#### 💎 总结 ####

​	总的来说，枚举是C语言中提升代码可读性和维护性的利器。它的核心优势在于将一组相关的整型常量**有组织地命名**，让代码的意图更明确。虽然C语言中的枚举类型检查相对宽松，但遵循良好的编程实践（如使用`typedef`、注意作用域）能让你更好地利用它。

